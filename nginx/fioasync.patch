diff --git a/src/os/unix/ngx_process.c b/src/os/unix/ngx_process.c
index 62e7b57..ff1c708 100644
--- a/src/os/unix/ngx_process.c
+++ b/src/os/unix/ngx_process.c
@@ -145,9 +145,9 @@ ngx_spawn_process(ngx_cycle_t *cycle, ngx_spawn_proc_pt proc, void *data,
         on = 1;
         if (ioctl(ngx_processes[s].channel[0], FIOASYNC, &on) == -1) {
             ngx_log_error(NGX_LOG_ALERT, cycle->log, ngx_errno,
-                          "ioctl(FIOASYNC) failed while spawning \"%s\"", name);
-            ngx_close_channel(ngx_processes[s].channel, cycle->log);
-            return NGX_INVALID_PID;
+                          "ioctl(FIOASYNC) failed while spawning \"%s\", ignore on OSv", name);
+            // ngx_close_channel(ngx_processes[s].channel, cycle->log);
+            // return NGX_INVALID_PID;
         }
 
         // No need to set owner for the socket since pid 0 will be running worker.
